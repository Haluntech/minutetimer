{
  "name": "minutetimer",
  "version": "1.0.0",
  "description": "Online clock and countdown timer",
  "main": "index.js",
  "scripts": {
    "start": "serve public",
    "build": "if not exist dist mkdir dist && xcopy /E /I /Y public dist",
    "deploy": "wrangler pages publish dist"
  },
  "dependencies": {
    "serve": "^14.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0",
    "wrangler": "^3.0.0"
  }
} 